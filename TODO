- ADD saliency extraction to rst-service.

- link nginx volume to local file system to avoid rebuild, recompiling


BUGS:

docker doesn't detect changes to volume, so what will be compiled from react and put in 
server www won't be update

ATTENTION:
